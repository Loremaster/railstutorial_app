<strong>Name</strong> <%= @user.name %><br />
<strong>URL</strong>  <%= link_to user_path(@user), @user %><br />			  		<%# Equal for "link_to user_path(@user), user_path(@user)". It is Rails convention. %>
<strong>Microposts</strong> <%= @user.microposts.count %>
<%= render 'shared/stats' %>