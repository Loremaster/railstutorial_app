<h2>
  Result: <strong>
             <%= @key_word %>
          </strong>
</h2>

<%= will_paginate %>
    <ul class="users">
        <%= render @searches %>																												<%# @users are objects of User. Automatically use each for this collection. Search partial _user.html.erb and take his code %>
    </ul>
<%= will_paginate %>